![[Pasted image 20260830144545.png]]![[Pasted image 20260830144609.png]]

## 问答题

### (Q3.1)

你认为，你在开发网络应用程序，与你在以往开发非网络应用程序的区别在哪里？网络应用程序的开发存在哪些额外的难点？存在哪些额外的复杂之处？

LocalCli 是全在一个进程里。这次调用 client.AnalyzeFilesAsync()，服务端在另一个进程，中间隔着网络。

我感觉那些要知道数据那些自动生成的方法和属性很麻烦，得去翻 proto 文件或 obj 里生成的代码（`FileNames` 还是 `LogFiles`、oneof 生成 `PayloadCase`）

### (Q3.2)

本次作业中，你是否使用了 AI？根据你的使用情况，在以下 (Q3.2.a) (Q3.2.b) 两个问题中选择一题作答：

#### (Q3.2.b)

帮我解释一些接口，帮我讲解那些 proto  自动生成的那些数据类型的属性和方法，也讲解一些框架，感觉仍然不是很明白，有一种被 ai 牵着走写代码的感觉（悲），仍然不太记得住和理解那些 C# 中的复杂的属性方法之类的名称和用法（晕），只能一边写一边求助 ai（

深刻的感觉到了我拖累了 ai 的水平（


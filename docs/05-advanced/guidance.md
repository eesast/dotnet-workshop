# Guidance for Advanced Functions

（还没写完，以下是计划）

预计分两大部分。

第一部分是给定选题，给定 N 个选题，要求学生选其中 M 个功能（或给每个选题赋予难度分，选其中至少 k 分，多余的分可以交给第二部分）。可以选的功能实现分为功能性以及美观性。比如可以选取至少 M1 个功能性、至少 M2 个美观性。

目前想到的有：

token 验证（类似 K8S 的 kubectl 的机制？）；日志更优显示；美化；多主题；parquet 文件支持（支持写入 parquet、读取 parquet）；移动端 UI 适配；持久化缓存修改结果，比较时间戳来决定是否重新分析（例如在 .analyzer 文件夹中缓存，并提供清除缓存功能，以及写入 hash 校验机制防止缓存与文件不一致）；云服务拓扑推断及显示（通过 Call 日志推断服务的调用关系，并在 GUI 前端显示拓扑图）……

第二部分是学生纯自由发挥，即可以实现第一部分给定选题中没做的选题，也可以自己想新的 idea。